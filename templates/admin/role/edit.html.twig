{% extends 'base.html.twig' %}

{% block title %}Редактирование роли{% endblock %}

{% block body %}
    <div class="container-fluid">
        <a href="{{ path('app_admin_role_index') }}" class="btn btn-link link-secondary p-0 mb-3 text-decoration-none">
            <i class="bi bi-arrow-left"></i> Вернуться к списку
        </a>
        <h1 class="mb-4">Редактирование роли: {{ role.name }}</h1>
        {{ include('_form.html.twig', {'button_label': 'Сохранить изменения'}) }}
    </div>
{% endblock %}