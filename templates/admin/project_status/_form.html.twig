{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="mt-3">
        <button type="submit" class="btn btn-success">{{ button_label|default('Сохранить') }}</button>
    </div>
{{ form_end(form) }}