{% extends 'base.html.twig' %}

{% block title %}Редактирование статуса{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1>Редактирование статуса проекта</h1>

    <div class="card card-body mt-3">
        {{ include('admin/project_status/_form.html.twig', {'button_label': 'Обновить статус'}) }}
    </div>
    <a href="{{ path('app_admin_project_status_index') }}" class="btn btn-link mt-3">Вернуться к списку</a>
</div>
{% endblock %}